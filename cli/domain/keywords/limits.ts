export const ASO_MAX_KEYWORDS = 100;

export const ASO_MAX_KEYWORDS_PER_CALL_ERROR =
  `A maximum of ${ASO_MAX_KEYWORDS} keywords is supported per call`;

export const ASO_MAX_KEYWORDS_PER_REQUEST_ERROR =
  `A maximum of ${ASO_MAX_KEYWORDS} keywords is supported per request.`;
